Weather app made with react using functional components and hooks. Uses framer-motion and react-reel libraries for animations. Styled-components for styling.

Uses openweatherapi 2.5 for weather data.
